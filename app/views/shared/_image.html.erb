<%  attachment_file = item_attachment.file
    file_path = thumb == true ? attachment_file.thumb.url : attachment_file.url
    image_path = item_attachment.is_image? ? file_path : attachment_file.default_url
%>

<%= image_tag(image_path) %>